# FastAPI-сервис для получения категорий новостных постов

Запуск сервера изнутри контейнера:
```bash
cd ./service
uvicorn app:app --host 0.0.0.0
```

Запуск сервера 


После сборки контейнера запуском команды ```docker compose up``` из корня репозитория доступен на <http://localhost:8080/docs>